# Blank React App

* Using of prettier  
    * Run prettier using terminal
    ```
        npx prettier src/App.js --write
    ```
    * Run prettier using terminal
        Config
        ```
            "scripts": {
                "format": "prettier --write \"src/**/*.{js,jsx}\""
            },
        ```
        Shortcut
        ```
            npm run format
        ```